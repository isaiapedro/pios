# Global Workspace Principles

Strict isolation of concerns.

Explicit dependency tracking via manifests.

Zero tolerance for data leakage between `personal/` and `knowledge/` or `services/`.

Favor strict local contracts over implicit global behavior.

Maintain historical continuity across all domains—append corrections rather than rewriting histories.
