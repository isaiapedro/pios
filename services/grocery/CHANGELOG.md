# Changelog

## Unreleased
- Local filesystem organization: isolated source routines to `src/` and asset scripts to `config/`.
- Setup environment variable configurations via local `.env` protection matrices.

---

## 1.0.0
Initial release of AWS Lambda-deployable Postgres processing engine.
