The base rounded surface used to group content.

```jsx
<Card padding={24}>conteúdo</Card>
<Card interactive onClick={() => {}}>clicável com elevação no hover</Card>
```
20px radius, warm 1px border, soft brown shadow.
