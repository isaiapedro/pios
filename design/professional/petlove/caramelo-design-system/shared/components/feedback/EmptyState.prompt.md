Centered empty / zero-result state with icon, copy and optional action.

```jsx
<EmptyState icon="cart" title="Sua sacola está vazia"
  description="Que tal começar pelos mais queridos?"
  action={<Button>Explorar produtos</Button>} />
```
