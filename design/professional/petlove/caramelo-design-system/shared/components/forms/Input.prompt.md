Labelled text field with focus ring, helper text and error state.

```jsx
<Input label="E-mail" placeholder="voce@email.com" helper="Usamos para o login" />
<Input label="CEP" error="CEP inválido" prefix={<Icon name="search" size={18} />} />
```
