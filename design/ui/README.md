# UI

Component contracts and design tokens reusable across PIOS surfaces.

## Product kits

| Folder | Kit |
|--------|-----|
| `chess-wrapped/` | Chess Wrapped primitives + token snapshot |

## Convention

Document **behavior + visual recipe**, not full JSX dumps. Point to source files for implementation. When forking into a new app, copy `theme.ts` first, then only the primitives you need.
