# Design System Principles

Form follows information density—prioritize clear data hierarchy.

Maintain access accessibility across diverse runtimes (CLI, Markdown widgets, Web views).

Ensure responsive layouts tailored to inputs (e.g., voice-to-text adjustments vs desktop editing formats).

Explicit design tokens over hard-coded CSS or style strings.

Preserve consistency—similar layout constructs should represent matching system behavior.
