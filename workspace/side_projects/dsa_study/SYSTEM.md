# Purpose

This project synchronizes public LeetCode study metadata into a local catalog,
renders an offline browser, and provides Python-first solution scaffolds.

It does not submit solutions, store credentials, or bypass paid-only access.
